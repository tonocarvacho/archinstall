    - name: install apps 
      become: true
      pacman:
        name:
          - spotify-launcher 
          - mpv
          - vifm
          - nemo
          - obs-studio
          - discord
          - docker
          - kitty
          - fzf
          - easyeffects
          - playerctl
          - rofi
      tags:
      - apps
      - install

    - name: install more apps with yay
      shell: runuser -l {{ ansible_user }} -c 'yay -S brave-bin google-chrome slack-desktop gummy --noconfirm' 
      tags:
      - apps   
      - install  

    - name: clean cache
      become: true
      shell: pacman -Sc 
      tags:
      - apps  
      - install    
